#ifndef FCFS_H
#define FCFS_H

#include "proc.h"   // to know the definition of struct proc

// Function prototype for FCFS scheduling
int fcfs(int proc_count, struct proc process[]);

#endif // FCFS_H
